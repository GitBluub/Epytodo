- from config import * -> no, eviter les imports
- retravailler naming (noms de variables/fonctions explicites)
- epytodo (Renommer app.app en epytodo.app ?)
- app factory (fonction qui retourne une application configured)
```
def main():
    app = epytodo.create_app()
    app.run()
if __name__ == '__main__':
    main()
```
https://instagram-engineering.com/python-at-scale-strict-modules-c0bb9245c834?gi=c6f4f9891e42
- unused arguments (`pycodestyle`, `pylint`)
- parentheses conditionnels et returns a eviter, tu fais pas du C
- trier les imports (`isort`)
- views.py -> regarder les blueprints, refacto facile
- sympa: mypy (type checking)
- sympa: flask sqlalchemy
- authentification: jwt (chronophage) / cookies (rapide)
- documenter le code (PEP257, PEP8)